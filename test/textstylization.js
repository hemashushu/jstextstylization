const assert = require('assert/strict');
const domino = require('domino');

const { TextStylization } = require('../index');

describe('TextStylization Test', () => {
    it('Test applyToRanges()', () => {
        // TODO::
    });
});
